# --- Database ---
DATABASE_URL=postgres://user:password@localhost:5432/business_oi
# --- App ---
APP_URL=http://localhost:3000
# --- Stripe (TEST MODE keys only until legal + merchant verification are complete) ---
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
# --- First admin (used only by `npm run db:seed`) ---
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=change-me-to-a-long-passphrase
